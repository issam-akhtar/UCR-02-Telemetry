database:
  connection_string: "postgres://postgres:H1a2m3z4a5%402003@localhost:5432/telem_db?sslmode=disable"

websocket:
  # For the sender: IP and port are used to build the connection URL.
  url: "ws://localhost:9090/telemetry"
  ip: "localhost"
  port: 9090  # Telemetry data port; receiver listens here (binding to all interfaces).

mode: "csv"             # Allowed values: "csv", "live"
apiport: "9000"         # API server port for the receiver.
dbc_file: "../../configs/UCR-01.dbc"
json_file: "../../configs/UCR-01.json"

throttler_interval: 0   # Throttler interval in milliseconds.
